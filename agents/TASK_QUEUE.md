# Agent Task Queue Pointer

Canonical task state lives in:

- `ai_context/TASK_QUEUE.md`

Agents must not maintain a separate task queue. Read and update only the canonical file above.

If task execution rules conflict with `skills.md`, follow `skills.md`.

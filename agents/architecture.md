# Agent Architecture Pointer

Canonical architecture lives in:

- `ai_context/architecture.md`

Agents must use this file only as a pointer. Do not duplicate or edit architecture content here.

If any agent pointer or context document conflicts with `skills.md`, follow `skills.md`.
